1. Clone the repository in your home directory
$ cd 
$ git clone git://github.com/roman/vimconfig.git

2. Then make a link of the vimrc config
$ ln -s ~/.vim/vimrc .vimrc

3. Expand the submodules to have the plugins working
$ cd ~/.vim
$ git submodules init

And then you are good to go :-)

